<%= turbo_stream.replace "character_area" do %>
    <%= render "users/character_display", user: current_user %>
  <% end %>
  
  <%= turbo_stream.replace "character_form_container" do %>
    <!-- 空にして閉じる -->
  <% end %>