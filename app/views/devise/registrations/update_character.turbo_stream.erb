<%= turbo_stream.update "character_area" do %>
  <%= render "devise/character_display", user: current_user %>
<% end %>

<%= turbo_stream.update "character_name_area" do %>
  <%= render "devise/character_name_display", user: current_user %>
<% end %>
  
<%= turbo_stream.update "character-form-container" do %>
  <!-- 空にして閉じる -->
<% end %>

<%= turbo_stream.update "flash" do %>
  <%= render "layouts/flash" %>
<% end %>